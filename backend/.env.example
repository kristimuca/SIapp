# Server Configuration
PORT=5000
NODE_ENV=development

# Encryption Key (256-bit key in hex format - 64 characters)
# Generate a new key using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-256-bit-key-here-64-hex-characters-long-example-key-value
