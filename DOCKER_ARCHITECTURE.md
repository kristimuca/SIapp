# 🐳 Docker Architecture Overview

## Container Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     Docker Host Machine                      │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │           Docker Compose (docker-compose.yml)          │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌────────────────────────┐  ┌──────────────────────────┐  │
│  │  Backend Container     │  │  Frontend Container      │  │
│  │  (encryption-backend)  │  │  (encryption-frontend)   │  │
│  │                        │  │                          │  │
│  │  ┌──────────────────┐ │  │  ┌────────────────────┐ │  │
│  │  │   Node.js 18     │ │  │  │  Nginx Alpine      │ │  │
│  │  │   Alpine Linux   │ │  │  │                    │ │  │
│  │  └──────────────────┘ │  │  └────────────────────┘ │  │
│  │                        │  │                          │  │
│  │  ┌──────────────────┐ │  │  ┌────────────────────┐ │  │
│  │  │ Express Server   │ │  │  │  React Build       │ │  │
│  │  │ Port: 5000       │ │  │  │  Static Files      │ │  │
│  │  │ /api/encrypt     │ │  │  │  Port: 80          │ │  │
│  │  │ /api/decrypt     │ │  │  │  Serves: 3000      │ │  │
│  │  │ /health          │ │  │  └────────────────────┘ │  │
│  │  └──────────────────┘ │  │                          │  │
│  │                        │  │  Depends on Backend ───────┤
│  │  Health Check: ✅     │  │                          │  │
│  └────────────────────────┘  └──────────────────────────┘  │
│           │                              │                   │
│           └──────────┬───────────────────┘                   │
│                      │                                       │
│         ┌────────────▼──────────────┐                       │
│         │  encryption-network       │                       │
│         │  (Bridge Network)         │                       │
│         └───────────────────────────┘                       │
│                                                               │
│  Ports Exposed:                                              │
│  - 5000:5000 → Backend API                                  │
│  - 3000:80   → Frontend UI                                  │
└─────────────────────────────────────────────────────────────┘

                           │
                           ▼
                    ┌──────────────┐
                    │   Browser    │
                    │              │
                    │ localhost:   │
                    │   3000       │
                    └──────────────┘
```

## Build Process Flow

```
┌─────────────────────────────────────────────────────────────┐
│                  docker-compose up --build                   │
└──────────────────────┬──────────────────────────────────────┘
                       │
           ┌───────────┴───────────┐
           │                       │
           ▼                       ▼
┌──────────────────────┐  ┌──────────────────────┐
│  Build Backend       │  │  Build Frontend      │
│                      │  │                      │
│  1. node:18-alpine   │  │  Stage 1:            │
│  2. Copy package.json│  │  1. node:18-alpine   │
│  3. npm ci           │  │  2. Copy package.json│
│  4. Copy app files   │  │  3. npm ci           │
│  5. Expose 5000      │  │  4. Copy app files   │
│  6. CMD node server  │  │  5. npm run build    │
│                      │  │                      │
│  Size: ~150MB        │  │  Stage 2:            │
│                      │  │  1. nginx:alpine     │
│                      │  │  2. Copy build files │
│                      │  │  3. Copy nginx.conf  │
│                      │  │  4. Expose 80        │
│                      │  │                      │
│                      │  │  Size: ~50MB         │
└──────────────────────┘  └──────────────────────┘
           │                       │
           └───────────┬───────────┘
                       │
                       ▼
           ┌───────────────────────┐
           │  Create Network       │
           │  encryption-network   │
           └───────────────────────┘
                       │
                       ▼
           ┌───────────────────────┐
           │  Start Containers     │
           │  - Backend first      │
           │  - Frontend depends   │
           └───────────────────────┘
                       │
                       ▼
           ┌───────────────────────┐
           │  Health Check         │
           │  Every 30 seconds     │
           └───────────────────────┘
                       │
                       ▼
                  ✅ Ready!
```

## Request Flow

```
┌──────────┐
│ Browser  │
│          │
└────┬─────┘
     │
     │ http://localhost:3000
     ▼
┌─────────────────────────┐
│  Frontend Container     │
│  (Nginx on port 80)     │
│                         │
│  Serves: React App      │
│  Static HTML/CSS/JS     │
└────┬────────────────────┘
     │
     │ User clicks "Encrypt"
     │
     │ POST http://localhost:5000/api/encrypt
     ▼
┌─────────────────────────┐
│  Backend Container      │
│  (Node.js on port 5000) │
│                         │
│  Express Server         │
│  ├─ Validates input     │
│  ├─ Encrypts with AES   │
│  └─ Returns data        │
└────┬────────────────────┘
     │
     │ {ciphertext, iv, authTag}
     ▼
┌─────────────────────────┐
│  Frontend Container     │
│  Displays result        │
└────┬────────────────────┘
     │
     ▼
┌──────────┐
│ Browser  │
│ Shows    │
│ Encrypted│
│ Data     │
└──────────┘
```

## Network Communication

```
                 Docker Bridge Network
                 (encryption-network)
                          
    ┌──────────────────────────────────┐
    │                                  │
    │  ┌────────────┐  ┌────────────┐│
    │  │  Backend   │  │  Frontend  ││
    │  │  Container │◄─┤  Container ││
    │  │            │  │            ││
    │  │  Internal: │  │  Internal: ││
    │  │  backend:  │  │  frontend: ││
    │  │  5000      │  │  80        ││
    │  └────────────┘  └────────────┘│
    │         ▲                ▲      │
    └─────────┼────────────────┼──────┘
              │                │
              │ Port           │ Port
              │ Mapping        │ Mapping
              │ 5000:5000      │ 3000:80
              │                │
         ┌────┴────────────────┴────┐
         │      Host Machine        │
         │   (Your Computer)        │
         └──────────────────────────┘
```

## Volume Mounts (None by Default)

```
Currently: No volumes mounted
(All data is ephemeral - lost when container stops)

To add persistence (optional):
┌──────────────────────────────────┐
│  docker-compose.yml              │
│                                  │
│  volumes:                        │
│    - ./backend/logs:/app/logs   │
│    - ./data:/app/data           │
└──────────────────────────────────┘
```

## Environment Variables Flow

```
┌──────────────┐
│  .env file   │
│              │
│  ENCRYPTION_ │
│  KEY=xxx...  │
└──────┬───────┘
       │
       │ docker-compose.yml reads
       ▼
┌──────────────────────┐
│  Docker Compose      │
│  Injects into        │
│  containers          │
└──────┬───────────────┘
       │
       ├───────────┬────────────┐
       │           │            │
       ▼           ▼            ▼
   Backend     Frontend    NODE_ENV
   Container   Container   
   
   ENCRYPTION  REACT_APP   production
   _KEY        _API_URL    
```

## Health Check Process

```
┌──────────────────────────┐
│  Docker Engine           │
│                          │
│  Every 30 seconds:       │
│                          │
│  wget --quiet --tries=1  │
│  http://localhost:5000   │
│       /health            │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│  Backend Container       │
│                          │
│  GET /health             │
│                          │
│  Returns:                │
│  {status: "OK",          │
│   timestamp: "..."}      │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│  Health Status           │
│                          │
│  ✅ Healthy              │
│  ❌ Unhealthy (3 fails)  │
│                          │
│  docker-compose ps       │
│  shows: Up (healthy)     │
└──────────────────────────┘
```

## Container Lifecycle

```
docker-compose up --build
         │
         ▼
    ┌────────┐
    │ Build  │
    │ Images │
    └───┬────┘
        │
        ▼
    ┌────────┐
    │ Create │
    │Network │
    └───┬────┘
        │
        ▼
    ┌────────┐
    │ Start  │
    │Backend │
    └───┬────┘
        │
        ▼
    ┌────────┐
    │ Start  │
    │Frontend│
    └───┬────┘
        │
        ▼
    ┌────────┐
    │ Health │
    │ Check  │
    └───┬────┘
        │
        ▼
    ┌────────┐
    │Running │ ◄──┐
    │  ✅    │    │
    └───┬────┘    │
        │         │
        │ Restart │
        │ unless- │
        │ stopped │
        └─────────┘

docker-compose down
         │
         ▼
    ┌────────┐
    │ Stop   │
    │Backend │
    └───┬────┘
        │
        ▼
    ┌────────┐
    │ Stop   │
    │Frontend│
    └───┬────┘
        │
        ▼
    ┌────────┐
    │ Remove │
    │Network │
    └───┬────┘
        │
        ▼
    ┌────────┐
    │ Done   │
    └────────┘
```

## File Structure in Containers

### Backend Container
```
/app/
├── node_modules/          (installed)
├── server.js              (copied)
├── routes.js              (copied)
├── encryptionService.js   (copied)
├── package.json           (copied)
└── package-lock.json      (copied)
```

### Frontend Container
```
/usr/share/nginx/html/
├── index.html             (from build)
├── static/
│   ├── css/              (from build)
│   ├── js/               (from build)
│   └── media/            (from build)
└── ...                   (other built files)

/etc/nginx/conf.d/
└── default.conf          (nginx.conf)
```

## Summary

| Aspect | Details |
|--------|---------|
| **Containers** | 2 (backend + frontend) |
| **Network** | 1 bridge network |
| **Volumes** | None (ephemeral) |
| **Ports** | 5000 (backend), 3000 (frontend) |
| **Base Images** | node:18-alpine, nginx:alpine |
| **Total Size** | ~200MB combined |
| **Health Checks** | Backend only (every 30s) |
| **Restart Policy** | unless-stopped |
| **Dependencies** | Frontend depends on backend |

---

**Ready to use Docker?** Run: `docker-compose up --build`

For detailed commands, see: `DOCKER_GUIDE.md`
